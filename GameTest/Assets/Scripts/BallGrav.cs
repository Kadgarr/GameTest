using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BallGrav : MonoBehaviour
{
    // Start is called before the first frame update
    public float GravityScale;
    void Start()
    {
        //GetComponent<Rigidbody>(). = GravityScale;
    }

   
}
